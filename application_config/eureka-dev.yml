server:
  port: 6868

eureka:
  client:
    #    //是否从eureka中获取注册信息
    fetch-registry: false
    #    是否将自己注册到eureka服务中
    register-with-eureka: false
    service-url:
      defaultZone: http://eureka6868.com:${server.port}/eureka/

